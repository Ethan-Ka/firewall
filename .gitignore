__pycache__/
*.py[cod]
.venv/
venv/
config.json
trunk-out/
recordings/
*.wav
*.m4a
.DS_Store
.env
.env.bak*
firewall.egg-info/
*.egg-info/
.geocache.json

# archived call audio (FIREWALL_AUDIO_DIR)
clips/
incidents/
corpus.jsonl

# web build toolchain. The tracker's build is not committed: the hosted copy is
# built by Vercel from web/, and a local one by ./scripts/run-tracker.sh.
node_modules/
web/node_modules/
web/dist/
.vercel/

# Vercel project credentials. Real push token; the deployment is configured
# from it, and it is not something the deployment gets to read.
.env.vercel
